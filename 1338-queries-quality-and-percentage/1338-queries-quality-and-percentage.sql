# Write your MySQL query statement below
select query_name,round(avg(rating/position),2)as quality,round(sum(if(q.rating<3,1,0))*100 /count(rating),2) as poor_query_percentage from queries q   where query_name is not null group by query_name;